# Vibe OS V4 — secret regex patterns (one per line, JS regex bodies, no flags).
# Mirrors scripts/secret-scanner.ts; kept in sync at release time.
# Lines starting with # are comments.

# Provider-specific
AKIA[0-9A-Z]{16}
sk_live_[0-9a-zA-Z]{24,}
pk_live_[0-9a-zA-Z]{24,}
sk-[a-zA-Z0-9]{20,}
ghp_[a-zA-Z0-9]{36}
gho_[a-zA-Z0-9]{36}
AIza[0-9A-Za-z_-]{35}

# Generic
-----BEGIN (RSA|DSA|EC|OPENSSH|PGP) PRIVATE KEY-----
(postgres|mysql|mongodb|redis):\/\/[^:\s]+:[^@\s]+@
