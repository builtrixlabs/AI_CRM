# Plan — 007-lead-lifecycle

## Files to be created

### Library

| File | Lines (~) | Purpose |
|---|---|---|
| `src/lib/leads/types.ts` | 30 | `LEAD_STATES` literal, `LeadState` type |
| `src/lib/leads/transitions.ts` | 90 | `TRANSITIONS` graph, `TERMINAL_STATES`, `allowedTransitions`, `assertTransitionAllowed`, `IllegalTransitionError`, `isTerminal` |
| `src/lib/leads/schemas.ts` | 60 | Zod payload schemas: `createLeadInputSchema`, `updateLeadInputSchema`, `transitionInputSchema` |
| `src/lib/leads/api.ts` | 200 | `createLead` (wraps `createNode`), `transitionLead` (state UPDATE + audit row with `{from,to,reason}`); accepts injectable client |
| `src/lib/leads/index.ts` | 15 | re-exports |

### Server actions

| File | Lines (~) | Purpose |
|---|---|---|
| `src/app/(dashboard)/dashboard/_actions/leads.ts` | 220 | `createLeadAction`, `updateLeadAction`, `transitionLeadAction` — auth + perm gate + Zod parse + dispatch + revalidatePath |

### UI components

| File | Lines (~) | Purpose |
|---|---|---|
| `src/components/dashboard/new-lead-dialog.tsx` | 150 | Client component. Trigger button + Dialog form (phone, source, email, notes); useFormState for Server Action |
| `src/components/canvas/edit-mode-button.tsx` | 35 | "Edit" toggle button (when `canEdit`) |
| `src/components/canvas/edit-lead-form.tsx` | 200 | Editable Header + Field block (replaces view mode); calls `updateLeadAction` |
| `src/components/canvas/transition-footer.tsx` | 130 | Renders allowed-state buttons; clicking forward → action; clicking terminal → opens reason dialog |
| `src/components/canvas/transition-reason-dialog.tsx` | 80 | Modal with required Textarea; submits via `transitionLeadAction` |

### App-route changes

| File | Change |
|---|---|
| `src/app/(dashboard)/dashboard/page.tsx` | Add "+ New lead" button gated on `leads:create`; mount `<NewLeadDialog>` |
| `src/app/(dashboard)/dashboard/leads/[id]/page.tsx` | Resolve user permissions; pass `canEdit` + `canTransition` props to `<LeadCanvas>` |
| `src/components/canvas/lead-canvas.tsx` | Add optional `canEdit`, `canTransition` props; mount `<EditModeButton>` / `<EditLeadForm>` / `<TransitionFooter>` accordingly. Defaults preserve D-006 behavior so the demo route is unaffected. |

### Tests

| File | Type | Lines (~) | Purpose |
|---|---|---|---|
| `tests/lib/leads/transitions.test.ts` | unit | 130 | state machine: every (from, to) pair; terminal-set membership; assertTransitionAllowed throws |
| `tests/lib/leads/schemas.test.ts` | unit | 70 | Zod validation for create / update / transition input shapes |
| `tests/lib/leads/api.test.ts` | unit | 220 | mocked supabase: createLead happy + label-from-phone fallback; transitionLead happy + IllegalTransitionError + missing-reason error + audit-row diff shape |
| `tests/components/dashboard/new-lead-dialog.test.tsx` | unit | 180 | renders form fields; opens/closes; submit calls action with FormData; field error rendering |
| `tests/components/canvas/edit-lead-form.test.tsx` | unit | 200 | initial values from lead; cancel reverts; submit calls action; Zod errors render inline |
| `tests/components/canvas/transition-footer.test.tsx` | unit | 200 | renders correct button set per state; terminal-state empty footer; click forward → action; click terminal → reason dialog opens |
| `tests/components/canvas/transition-reason-dialog.test.tsx` | unit | 90 | rejects empty reason; submits with reason text |
| `tests/components/canvas/lead-canvas-extras.test.tsx` | unit | 120 | with `canEdit`+view: EditButton visible, no form. with `editing` state: form replaces view. with `canTransition`: footer renders. demo route: nothing extra renders. |
| `tests/app/dashboard/_actions/leads.test.ts` | unit | 250 | server actions: 401 unauth, 403 missing perm, 422 bad data, 200 happy. Mock getCurrentUser + clients. |
| `tests/integration/lead-create-edit-transition.test.ts` | integration | 280 | seed sales_rep in workspace W; create lead → assert row + audit; edit lead → assert update + audit; transition new→contacted→qualified→lost → assert state changes + audit rows with {from,to,reason} shape; cross-tenant rep can't update |
| `tests/e2e/lead-create-flow.spec.ts` | e2e @smoke | 80 | from /dashboard, click "+ New lead", fill, submit, land on /dashboard/leads/<id> with state="new" |
| `tests/e2e/lead-edit-and-transition.spec.ts` | e2e @smoke | 100 | edit fields + save; click "Mark contacted"; state badge updates; click "Mark lost" → reason dialog → submit; footer becomes terminal |

## Files to be modified

| File | Change |
|---|---|
| `package.json` | shadcn `Dialog`, `Select`, `Textarea` (if not present) |
| `components.json` | regenerated by shadcn CLI |
| `src/components/ui/dialog.tsx` | added by shadcn (NEW; auto-generated) |
| `src/components/ui/select.tsx` | added by shadcn (NEW; auto-generated) |
| `src/components/ui/textarea.tsx` | added by shadcn (NEW; auto-generated) |
| `memory/decisions.md` | append D-007.x entries (Group D) |
| `memory/learned/ai-crm/patterns.md` | append patterns (Group D) |

> Note: `Dialog` is already installed (D-001 used it) — no re-install needed.
> `Select` and `Textarea` need to be added.

## Migrations

**None.** D-002's `nodes` table covers all D-007 needs:
- `state` column → updated by `transitionLead`
- `data jsonb` → updated by `updateLead`
- provenance + audit_log already enforced by D-002's helpers

## Tests (TDD order)

1. **State machine** first (`transitions.test.ts`) — pure function, fast.
2. **Zod schemas** (`schemas.test.ts`) — pure validation.
3. **API helpers** (`api.test.ts`) — `createLead`, `transitionLead`
   with mocked supabase clients.
4. **Server actions** (`_actions/leads.test.ts`) — auth + perm + dispatch.
5. **UI components** in dependency order:
   - `transition-reason-dialog.test.tsx` (leaf)
   - `transition-footer.test.tsx` (uses reason dialog)
   - `edit-lead-form.test.tsx` (uses Zod schemas)
   - `new-lead-dialog.test.tsx` (entry surface)
   - `lead-canvas-extras.test.tsx` (canvas wiring with new props)
6. **Integration** — seed against bwumqahgwobwghlmzcrl; full create →
   edit → transition flow; cross-tenant denial.
7. **Playwright @smoke** — two specs covering the operator flow.

## Coverage estimate

- **Lines** ≥ 80% on `src/lib/leads/`,
  `src/components/canvas/edit-mode-button.tsx`,
  `src/components/canvas/edit-lead-form.tsx`,
  `src/components/canvas/transition-footer.tsx`,
  `src/components/canvas/transition-reason-dialog.tsx`,
  `src/components/dashboard/new-lead-dialog.tsx`. Realistic ~88%.
- **Branches** ≥ 90%. Realistic ~92% (state machine is many small branches,
  all easy to hit).
- **Stretch (`@stretch`)** — none planned.

## Risks (for Plan Mode reviewer)

| # | Risk | Severity | Mitigation |
|---|---|---|---|
| P-1 | Stacked PR off `feature/006`. Rebase needed if D-006 PR changes. | Med | D-007 only adds files + adds optional props to lead-canvas.tsx — minimal touch on D-006 surface. |
| P-2 | `transitionLead` doesn't use `updateNodeData` because it needs a different audit diff (`{from,to,reason}` vs `{before,after}`). Two audit-write paths now exist. | Med | Encapsulate in `src/lib/leads/api.ts`; integration test asserts the audit row shape. Document in decisions D-007.x. |
| P-3 | shadcn Select + Textarea install (first instance) may snag CSS variables / theme. | Low | Use V5's `shadcn-component-install` skill; verify build. |
| P-4 | Edit-mode toggles the entire canvas (no inline per-field). Some users might expect inline. | Low | Documented out-of-scope; per-field inline is V1. |
| P-5 | useFormState / useActionState API churn between Next 15 and 16. Server Action error UX needs the right hook. | Low | Use `useActionState` (Next 16); has stable typed-action contract. |
| P-6 | Hidden FormData injection — a forged client could submit `lead_id` for a different tenant. | Med | `transitionLead` reads the lead via service-role + checks `organization_id = user.org_id` BEFORE the UPDATE. Integration-tested. |
| P-7 | Reason dialog can be bypassed (forged FormData omitting `reason`) for terminal transitions. | Med | Server-side validation in `transitionInputSchema`: `reason` is required when target is terminal. Refine schema with `.superRefine`. |
| P-8 | Sticky terminals → user accident. | Low | V0 docs; reactivate is V1. |

## Out-of-scope reaffirmation

D-007 does NOT ship:

- Deal canvas + deal-side transitions (V1)
- "Promote to deal" flow at qualified state (V1)
- "Reactivate" from terminal (V1)
- Soft-delete UI (V1; helper exists)
- Bulk lead operations (V1+)
- Reassignment between reps / workspaces (V1)
- Cmd+K creation (D-008)
- Custom fields engine (D-112)
- Pipeline stage editor (V2 per Constitution XI L3)
- Activity-typed nodes auto-created on edit (only audit_log gets the
  edit; activity nodes come from inbound channels in D-010 / D-013)
- Optimistic UI on transition click
- Workspace selector when user has multiple workspaces
- Lead canvas access for super_admin (RLS-blocked per D-001.12)
